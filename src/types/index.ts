export interface Destination {
  title: string;
  image: string;
  description: string;
  itinerary?: string;
}

export interface Testimonial {
  name: string;
  text: string;
  rating: number;
  image: string;
}